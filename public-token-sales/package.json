{
  "name": "launchpad",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --network mainnet --ipfs https://graph.vefinetwork.org/app/ipfs/ --node https://graph.vefinetwork.org/app/polygon/admin/ vefi/public-token-sale",
    "create-remote": "graph create --network mainnet --node https://graph.vefinetwork.org/app/polygon/admin/ vefi/public-token-sale",
    "remove-local": "graph remove --node http://localhost:8020/ vefi/launchpad",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vefi/launchpad",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
